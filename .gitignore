# Laufprotokoll der Studien-Automatik.
# Bleibt bewusst lokal: Alles im Repo wird von GitHub Pages oeffentlich
# ausgeliefert, das Log gehoert nicht auf die Website.
update.log

# Fremde Dateien, die im Ordner liegen, aber nicht zu dieser Website
# gehoeren. Alles im Repo wird oeffentlich ausgeliefert - was hier nicht
# hingehoert, darf gar nicht erst versioniert werden.
functions/
intern/
